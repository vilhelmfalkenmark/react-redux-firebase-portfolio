{
  "name": "villes-portfolio-server",
  "version": "1.0.0",
  "description": "heroku server",
  "engines": {
    "node": "6.2.2"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-redux-firebase-portfolio/ && npm install --only=dev && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "multer": "^1.2",
    "request": "^2.79.0",
    "request-promise": "^4.1.1",
    "del": "^2.2.2"
  },
  "repository": {
    "type": "git"
  },
  "keywords": [
    "node",
    "heroku",
    "create-react-app",
    "react"
  ],
  "license": "MIT",
  "devDependencies": {}
}
